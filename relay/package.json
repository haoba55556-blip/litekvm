{
  "name": "litekvm-relay",
  "version": "0.1.0",
  "private": true,
  "description": "LiteKVM self-hosted cross-network relay (token-authenticated, content-agnostic TCP forwarder)",
  "license": "GPL-2.0",
  "engines": {
    "node": ">=18"
  },
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "node test/selftest.js",
    "derive-token": "node tools/derive-token.js"
  },
  "dependencies": {},
  "repository": {
    "type": "git",
    "url": "https://github.com/<your-fork>/litekvm"
  }
}
